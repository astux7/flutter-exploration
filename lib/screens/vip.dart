import 'package:flutter/material.dart';
import '../tiles.dart';

class VipHub extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ListView(
      children: [
        MessageCenterTile()
      ],
    );
  }
}

